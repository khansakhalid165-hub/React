Q)What is react?
React is a open source javascript library use to build fast,intercative and reusable interface expecially for a single page application
In another words it helps the developer buld mordern websites more efficiently
Q)Why react created?
Before react developer buld websites using only HTML,CSS,javascript this worked well for small websites but  as mordern applications beacm emore interactive so react is much needed,these applications constantly update  their content without reloading the whole page .Managing these websites with plain js become more ifficult
Problems with Js are:
imagine you are building websites every second user like posts,comment,follow people,recieve notification,send messages  using JS you have to manually update it 
example 
const like=documnet.getelementid("likes)
like.innertext="101 likes'
every update require finding elemnts ,changing texts,creating new elements and deleting old elements
As the application grow the code become long,repetitive,hard to maintain,more likely to contain bugs
React solve this problem instead of telling the browser updaste the paragraph it tell the data has changes  react automatically updates the UI
React has usable componensts,Better code optimization,easier mainatinence,
the limitation of react is only handles the userinterface, need other library for routing and state management